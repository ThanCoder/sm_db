# 0.2.2

- Fixed `Some Errors`

## 0.2.1

- Added `getListByParentId` in `Box<T>` - **_ Very Fast _**
- Added `updateById` in `Box<T>`
- Added `updateOne` in `Box<T>`
- Added `getOne` in `Box<T>`
- Added `getOneStream` in `Box<T>`
- Fixed `Not Found Classes` In Lib

## 0.1.0

- Initial version.
